{
  "name": "ghostd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "pnpm native:dev",
    "client:dev": "sh scripts/portless.sh ghostd.wterm vite --config web/vite.config.ts",
    "native:dev": "pnpm build && sh scripts/portless.sh ghostd.wterm pnpm native:run",
    "native:build": "pnpm build:web && sh scripts/zig-015.sh build --cache-dir .zig-cache --global-cache-dir .zig-cache",
    "native:run": "pnpm build:web && sh scripts/zig-015.sh build run --cache-dir .zig-cache --global-cache-dir .zig-cache",
    "native:test": "pnpm build:web && sh scripts/zig-015.sh build test --cache-dir .zig-cache --global-cache-dir .zig-cache",
    "build:deps": "pnpm --filter @wterm/core build && pnpm --filter @wterm/dom build",
    "build:web": "vite build --config web/vite.config.ts && node scripts/generate-embedded-assets.mjs",
    "build": "pnpm build:deps && pnpm build:web",
    "type-check": "pnpm --filter @wterm/core type-check && pnpm --filter @wterm/dom type-check && tsc --project web/tsconfig.json --noEmit",
    "test": "pnpm --filter @wterm/core build && pnpm --filter @wterm/dom test && pnpm native:test && pnpm e2e",
    "e2e": "playwright test",
    "preview": "vite preview --config web/vite.config.ts"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "@wterm/core": "workspace:^",
    "@wterm/dom": "workspace:^"
  },
  "devDependencies": {
    "@internal/ts": "workspace:*",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "packageManager": "pnpm@11.1.3"
}
